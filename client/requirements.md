## Packages
framer-motion | Complex UI transitions and layout animations
date-fns | Date and duration formatting
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
- MediaRecorder API usage requires HTTPS in production (works on localhost)
- Upload endpoint expects multipart/form-data with 'file', 'title', 'duration'
- Theme is set to dark mode by default for a cinematic feel
